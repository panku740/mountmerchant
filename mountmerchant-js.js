document.addEventListener("DOMContentLoaded",()=>{const body=document.body;const header=document.querySelector("[data-site-header]");const menuButton=document.querySelector(".mobile-menu-btn");const nav=document.querySelector("#site-nav");const submenuButtons=document.querySelectorAll(".submenu-toggle");const setHeader=()=>header?.classList.toggle("is-scrolled",window.scrollY>12);setHeader();window.addEventListener("scroll",setHeader,{passive:true});menuButton?.addEventListener("click",()=>{const open=!body.classList.contains("nav-open");body.classList.toggle("nav-open",open);menuButton.setAttribute("aria-expanded",String(open));menuButton.setAttribute("aria-label",open?"Close navigation":"Open navigation")});nav?.addEventListener("click",event=>{const link=event.target.closest("a");if(link&&window.matchMedia("(max-width: 980px)").matches){body.classList.remove("nav-open");menuButton?.setAttribute("aria-expanded","false");menuButton?.setAttribute("aria-label","Open navigation")}});submenuButtons.forEach(submenuButton=>{const dropdown=submenuButton.closest(".dropdown-parent")?.querySelector(".dropdown");if(!dropdown) return;submenuButton.addEventListener("click",event=>{event.stopPropagation();const open=!dropdown.classList.contains("is-open");dropdown.classList.toggle("is-open",open);submenuButton.setAttribute("aria-expanded",String(open))})});const reveals=document.querySelectorAll(".reveal");if("IntersectionObserver"in window){const observer=new IntersectionObserver(entries=>{entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add("is-visible");observer.unobserve(entry.target)}})},{threshold:.14});reveals.forEach(item=>observer.observe(item))}else reveals.forEach(item=>item.classList.add("is-visible"))});
